module.exports = {
    siteUrl: 'https://expat.tours',
    generateRobotsTxt: true,
    sitemapSize: 5000,
    changefreq: 'daily',
    priority: 0.7,
  };
    // This is optional, but you can specify the output directory for the sitemap files
    // sitemapOutputDir: 'public/sitemaps',
    // This is optional, but you can specify the output directory for the robots.txt file
    // robotsTxtOutputDir: 'public',
    // This is optional, but you can specify the output directory for the sitemap index file
    // sitemapIndexOutputDir: 'public/sitemaps',
    // This is optional, but you can specify the output directory for the sitemap files
    // sitemapFilesOutputDir: 'public/sitemaps',