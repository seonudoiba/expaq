spring.application.name=expaq

spring.security.user.name=abiodun
spring.security.user.password=admin

# PostgreSQL Database Configuration (Local - Laragon)
spring.datasource.url=jdbc:postgresql://localhost:5432/expaqdb
# Default Laragon port
spring.datasource.username=postgres
# Default Laragon username
spring.datasource.password=admin
# Set your Laragon PostgreSQL password here

# PostgreSQL Database Configuration (Production - Example using Env Vars)
# spring.datasource.url=${JDBC_DATABASE_URL} # Example: jdbc:postgresql://prod-db-host:5432/prod_expaqdb
# spring.datasource.username=${JDBC_DATABASE_USERNAME}
# spring.datasource.password=${JDBC_DATABASE_PASSWORD}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
# Be cautious with 'update' in production, consider 'validate' or 'none'
spring.jpa.show-sql=true
# Set to false in production for cleaner logs
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Ensure JSONB type handling if using Hibernate 6+
# spring.jpa.properties.hibernate.type.json_format_mapper=com.fasterxml.jackson.databind.ObjectMapper

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=/tmp
logging.level.org.springframework.security=TRACE

# Cloudinary Configuration
cloudinary.cloud-name=do0rdj8oj
cloudinary.api-key=678796867796868
cloudinary.api-secret=yjMJDocSLyJp_T4wxSAAZvFc_Xc

